import { CourseList } from "@/features/admin/components/course-list";

export const metadata = {
  title: "Cursos | TBO Academy Admin",
};

export default function AdminCursosPage() {
  return <CourseList />;
}
